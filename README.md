# Wicken NFT contract deploying: Hardhat Project

Try running some of the following tasks:

```shell
npm install 
npx hardhat compile
npx hardhat run scripts/deploy.js --network rinkeby
node scripts/mint-nft.js
```
